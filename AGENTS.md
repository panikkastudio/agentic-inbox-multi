## Agent skills

### Issue tracker

Issues are Markdown files under `.beans/`, managed with the `beans` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage roles are stored as Beans tags. See `docs/agents/triage-labels.md`.

### Domain docs

This repo uses a single-context domain-doc layout. See `docs/agents/domain.md`.
